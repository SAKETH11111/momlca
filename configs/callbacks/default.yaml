defaults:
  - model_checkpoint_best
  - model_checkpoint_last
  - early_stopping
  - model_summary
  - rich_progress_bar
  - _self_

early_stopping:
  monitor: "val/loss"
  patience: 100
  mode: "min"

model_summary:
  max_depth: -1
