# XGBoost baseline configuration
_target_: gnn.baselines.models.XGBoostBaseline

property_names:
  - logS
  - logP
  - pKa

n_estimators: 100
max_depth: 6
learning_rate: 0.1
subsample: 0.8
colsample_bytree: 0.8
random_state: ${seed}
