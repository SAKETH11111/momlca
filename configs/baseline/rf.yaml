# Random Forest baseline configuration
_target_: gnn.baselines.models.RandomForestBaseline

property_names:
  - logS
  - logP
  - pKa

n_estimators: 100
max_depth: null  # Unlimited depth
min_samples_split: 2
n_jobs: -1  # Use all cores
random_state: ${seed}
